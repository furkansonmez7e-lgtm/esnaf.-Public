import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Önizleme — Esnaf",
};

export default function OnizlemeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
